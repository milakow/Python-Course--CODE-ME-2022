#Poznaj trójkąt Pascala. Napisz kod, który wyświetla trójkąt Pascala dla podanego N.
N = input('Pick a number N: ')
value = []

for index, value in enumerate(N):
    value = []
    len(value) == index
    value[0] == value[-1] == 1
    print(value)


